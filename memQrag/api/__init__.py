"""API module boundary (placeholder).

Planned responsibility, per docs/ARCHITECTURE.md: the FastAPI app, request
and response schemas, endpoint handlers, and dependency wiring.

The FastAPI app with a health endpoint lands in the next Phase 1 PR (see
docs/PRODUCT_TIMELINE.md). No app exists yet.
"""
